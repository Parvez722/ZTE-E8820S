name: PyTorch Cloudflared Server

on:
  workflow_dispatch:

jobs:
  run-pytorch:
    runs-on: ubuntu-20.04

    steps:
      - name: Update system
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip curl

      - name: Install PyTorch (CPU)
        run: |
          pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

      - name: Install Flask (for web server)
        run: |
          pip3 install flask

      - name: Create simple PyTorch web app
        run: |
          cat << 'EOF' > app.py
          from flask import Flask
          import torch

          app = Flask(__name__)

          @app.route("/")
          def home():
              x = torch.tensor([1, 2, 3])
              return f"PyTorch working! Tensor: {x}"

          if __name__ == "__main__":
              app.run(host="0.0.0.0", port=80)
          EOF

      - name: Install Cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared

      - name: Run server and tunnel
        run: |
          python3 app.py &
          sleep 5
          ./cloudflared tunnel run --token eyJhIjoiMmIwMDljMmM0Y2RkYTEzMmI5ODY3YmE5ZmE1YWZkODciLCJ0IjoiZTk2MjRmMDEtMDczNS00OTUwLWFiN2QtNTVkYzMzZTY4NjU3IiwicyI6IllqZ3hNek5rTmpFdFpHTm1OQzAwTURCaUxUaG1ZVFV0Wm1WaE1qZGlOV1l5WXpBeiJ9
